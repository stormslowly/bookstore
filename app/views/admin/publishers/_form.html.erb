<%= form_for(@admin_publisher) do |f| %>
  <% if @admin_publisher.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_publisher.errors.count, "error") %> prohibited this admin_publisher from being saved:</h2>

      <ul>
      <% @admin_publisher.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
